name: First workflow
on: workflow_dispatch
jobs:
  first_job:
    runs_on: ubuntu-latest
    steps:
      - name: Print Greetings
        run: echo "Hello world!"
      - name: Print Goodbye
        run: echo "Done -byee!"
